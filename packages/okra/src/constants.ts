import type { Metadata } from "./store.js"

export const OKRA_VERSION = 1

export const DEFAULT_K = 16
export const DEFAULT_Q = 32
export const DEFAULT_METADATA: Metadata = { K: DEFAULT_K, Q: DEFAULT_Q }
