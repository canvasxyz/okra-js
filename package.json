{
	"private": true,
	"description": "A p2p merklized database index",
	"version": "0.1.0",
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "tsc --build --watch",
		"build": "tsc --build",
		"clean": "tsc --build --clean",
		"test": "pnpm -r test",
		"prepublishOnly": "echo 'Do not use npm publish! Use pnpm publish instead.' && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git://github.com/canvasxyz/okra-js.git"
	},
	"dependencies": {
		"@canvas-js/okra": "workspace:*",
		"@canvas-js/okra-lmdb": "workspace:*",
		"@canvas-js/okra-memory": "workspace:*"
	},
	"devDependencies": {
		"@ava/typescript": "^5.0.0",
		"@types/node": "^22.13.10",
		"ava": "^6.2.0",
		"prettier": "^3.5.3",
		"typescript": "^5.8.2"
	}
}
